name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  packages: write

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc

      - uses: pnpm/action-setup@v5

      - name: Install frontend deps
        run: pnpm install

      - name: Install lint tools
        run: |
          go install github.com/golangci/golangci-lint/v2/cmd/golangci-lint@latest
          pip install yamllint

      - name: golangci-lint
        run: golangci-lint run --config .golangci.yml --timeout 5m ./cmd/... ./internal/...

      - name: yamllint
        run: |
          find . -name '*.yaml' -o -name '*.yml' \
            | grep -v node_modules | grep -v pnpm-lock | grep -v bazel- \
            | xargs yamllint --strict -c .yamllint.yaml

      - name: eslint
        run: pnpm --filter @devops-course/web lint
        working-directory: .

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc

      - uses: pnpm/action-setup@v5

      - name: Install frontend deps
        run: pnpm install

      - name: Go tests
        run: go test -v -race -coverprofile=coverage.out ./internal/... ./cmd/...

      - name: Frontend tests
        run: pnpm --filter @devops-course/web test

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc

      - uses: pnpm/action-setup@v5

      - name: Install frontend deps
        run: pnpm install

      - name: Build Go backend
        run: go build -o bin/server ./cmd/server

      - name: Build Astro frontend
        run: pnpm --filter @devops-course/web build

  bazel:
    name: Bazel Build & Test
    runs-on: ubuntu-latest
    needs: [lint, test, build]
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc

      - uses: pnpm/action-setup@v5

      - name: Install frontend deps
        run: pnpm install

      - uses: bazel-contrib/setup-bazel@0.14.0
        with:
          bazelisk-cache: true
          disk-cache: ${{ github.workflow }}
          repository-cache: true

      - name: Bazel build
        run: bazel build //... --config=ci

      - name: Bazel test
        run: bazel test //internal/... //cmd/... --config=ci

  contracts:
    name: Contract Validation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc

      - uses: pnpm/action-setup@v5

      - name: Install dependencies
        run: pnpm install

      - name: Lint OpenAPI spec
        run: pnpm --filter @devops-course/contracts lint

      - name: Check Go codegen freshness
        run: |
          make generate-go
          git diff --exit-code packages/contracts/generated/go/ || \
            (echo "::error::Generated Go code is out of date. Run 'make generate-go' and commit." && exit 1)

      - name: Check TypeScript codegen freshness
        run: |
          make generate-ts
          git diff --exit-code packages/contracts/generated/ts/ || \
            (echo "::error::Generated TS code is out of date. Run 'make generate-ts' and commit." && exit 1)

      - name: Detect breaking changes
        if: github.event_name == 'pull_request'
        run: |
          go install github.com/oasdiff/oasdiff@latest
          git fetch origin main
          git show origin/main:packages/contracts/openapi/api.yaml > /tmp/api-base.yaml 2>/dev/null || exit 0
          oasdiff breaking /tmp/api-base.yaml packages/contracts/openapi/api.yaml

  e2e:
    name: E2E Tests
    runs-on: ubuntu-latest
    needs: [bazel]
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc

      - uses: pnpm/action-setup@v5

      - name: Install dependencies
        run: pnpm install

      - name: Build frontend
        run: pnpm --filter @devops-course/web build

      - name: Start Go backend
        run: go run ./cmd/server &
        env:
          PORT: "8080"

      - name: Start Astro preview server
        run: |
          cd apps/web && pnpm preview --port 4321 &
          # Wait for both servers to be ready
          for i in $(seq 1 30); do
            curl -sf http://localhost:8080/healthz > /dev/null 2>&1 && break
            sleep 1
          done
          for i in $(seq 1 30); do
            curl -sf http://localhost:4321/ > /dev/null 2>&1 && break
            sleep 1
          done

      - name: Run Cypress tests
        run: pnpm --filter @devops-course/e2e test
        env:
          CYPRESS_API_URL: http://localhost:8080

      - name: Upload Cypress screenshots on failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: cypress-screenshots
          path: e2e/cypress/screenshots/
          retention-days: 7

  image:
    name: Build & Push Images
    runs-on: ubuntu-latest
    needs: [lint, test, build, bazel, contracts]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    steps:
      - uses: actions/checkout@v6

      - uses: bazel-contrib/setup-bazel@0.14.0
        with:
          bazelisk-cache: true
          disk-cache: ${{ github.workflow }}-image
          repository-cache: true

      - uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc

      - uses: pnpm/action-setup@v5

      - name: Install frontend deps
        run: pnpm install

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push server image
        run: |
          bazel run //cmd/server:push --config=ci \
            -- --tag "${{ github.sha }}" --tag "latest"

      - name: Build and push web image
        run: |
          bazel run //apps/web:push --config=ci \
            -- --tag "${{ github.sha }}" --tag "latest"
